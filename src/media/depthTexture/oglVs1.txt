!!ARBvp1.0

ATTRIB iPos = vertex.position;
ATTRIB iTex = vertex.texcoord;
PARAM scale = { 2, -2, -1, 1 };
TEMP tmp;

MOV tmp.xy, iPos;
MAD tmp.xy, tmp.xyyy, scale.xyyy, scale.zwww;
MOV tmp.zw, iPos.zwww;
MOV result.position, tmp;
RCP tmp, iPos.w;
MUL result.texcoord, iPos.z, tmp;

END
