# Assignment: Windows Docker build environment and CircleCI migration

---
## Goals
Build and verify a Windows dependency image, publish it as
randomgraphics/garnet-windows:<revision>, then move Windows CI to CircleCI.

---
## Design
Use Windows Server Core 2022 with VS 2026 C++ Build Tools, Windows SDK,
current stable Git/LFS, CMake, Python and the official LunarG Vulkan SDK. CircleCI
must use a Windows machine executor to run this Windows container.
Keep image scripts in env/docker/windows. Publish only after all three
build variants pass. Preserve existing CI until verification/publication.

---
--- PROGRESS (resume from "Last completed" / "In progress") ---
Last completed: Phase 1.2 — built current dependency image and passed debug/profile/release
  Phase 0: Docker reports linux; no Windows Docker context is configured
  Phase 1.1: all 8 PowerShell scripts parse; Docker preflight mock checks and git diff --check pass
  Phase 1.2: Windows image 6f465e79df29 verified on 2026-09-07; all three builds passed
In progress: Phase 2.1 — uploading verified image to Docker Hub
--- END PROGRESS ---
--- REFERENCE ---
.github/workflows/windows-ci.yml
.circleci/config.yml
env/docker/ubuntu/
env/vulkan-sdk-version.txt
env/requirements.txt

--- BUILD / CI ---
On a Windows container host, from the repository root:
  powershell -ExecutionPolicy Bypass -File env/docker/windows/build.ps1
  powershell -ExecutionPolicy Bypass -File env/docker/windows/verify.ps1
  docker login
  powershell -ExecutionPolicy Bypass -File env/docker/windows/upload.ps1

--- WORKFLOW / CONVENTIONS ---
User requested saving the draft and plan on wip/ci/windows-docker, committing
and pushing for continuation on a Windows host.
Docker Hub publication is explicitly requested, after successful verification.
Do not report Linux static checks as Windows build verification.
Verification (2026-09-07): Docker Desktop 4.90.0 / engine 29.7.2 runs Windows
containers with storage at D:\dev\docker\windows. An initial base pull failed
with unexpected EOF; restarting Docker Desktop and retrying succeeded.
Image: randomgraphics/garnet-windows:00001
Image ID: sha256:6f465e79df29549327f856ff62c3fc31228238a70b170f1eb0f64ab515b4173f
Verified UTC: 2026-09-07T20:22:45.3053309Z
Versions: VS 2026 18.9.2, MSVC 19.51.36256.0, Windows SDK 10.0.26100.16,
Git 2.55.0.windows.5, LFS 3.8.0, CMake 4.4.3, Python 3.14.7, Vulkan 1.4.357.0.
Python 3.12 was an undocumented draft pin, not a demonstrated requirement.
OpenEXR 3.4.15 lacks a cp314 Windows wheel but built successfully from source
with MSVC. All Python requirements installed, pip check passed, and resolved
versions are recorded inside the image at C:\image\python-packages.txt.
All 8 PowerShell scripts parse, git diff --check passes, and build.ps1 plus
verify.ps1 passed. Debug/profile/release each compiled in a fresh container.
Checkout submodules and LFS assets are hydrated. No GPU runtime tests were run.
The existing GitHub CI workflow and shared Vulkan version pin are unchanged.
Docker Hub upload is underway; pull and record the digest before CI migration.

--- TASK BREAKDOWN ---
Phase 1: Prepare and verify the image
─────────────────────────────────────────────────────
  Task 1.1: Add Dockerfile, dependency installation, and lifecycle scripts
    Verify: Review scripts and run available static checks.
    Commit: "ci(windows): prepare Windows container tooling (Phase 1.1)"
  Task 1.2: Build image and compile debug, profile, release in containers
    Verify: build.ps1 and verify.ps1 succeed on a Windows Docker host.
    Commit: "ci(windows): verify Windows container builds (Phase 1.2)"
Phase 2: Publish and migrate CI
─────────────────────────────────────────────────────
  Task 2.1: Publish verified revision to Docker Hub
    Verify: upload.ps1 succeeds; pull and inspect published image digest.
    Commit: "ci(windows): publish verified image revision (Phase 2.1)"
  Task 2.2: Add Windows CircleCI matrix and retire Windows GitHub workflow
    Verify: CircleCI configuration validates; all Windows jobs pass.
    Commit: "ci(windows): move Windows builds to CircleCI (Phase 2.2)"

--- SUMMARY OF FILES TO TOUCH ---
Phase 1 (image tooling):
  env/docker/windows/ — NEW: image, lifecycle scripts and documentation
Phase 2 (migration):
  .circleci/config.yml — Windows machine executor and container build matrix
  .github/workflows/windows-ci.yml — retire after CircleCI verification
