project
    : requirements
        <include>src/priv/inc
        <variant>debug:<define>GN_DEBUG=1
        <variant>release:<define>NDEBUG
        <variant>profile:<define>GN_ENABLE_PROFILE=1
        #<toolset>msvc:<cflags>"/W4 /WX"
        #<toolset>gcc:<cflags>"-Werror -Wall"
    : build-dir tmp/bjam
    ;

path-constant TOP : . ;

build-project src/priv/base ;
build-project src/extern/src ;
