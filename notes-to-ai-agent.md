- do not call cmake direclty, use build.py instead, to build the project
- only include minimal amount of headers. For example, header A includes header B, when both A and B are needed, include A is enough.

